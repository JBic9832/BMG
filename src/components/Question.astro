---
interface Props {
    question: string;
    answer: string;
}

const { question, answer } = Astro.props;
---

<div class="flex flex-col gap-2">
    <h1 class="text-2xl 2xl:text-4xl font-semibold">{question}</h1>
    <p class="text-base 2xl:text-xl">{answer}</p>
</div>
